apiVersion: serving.knative.dev/v1beta1
kind: DomainMapping
metadata:
  name: hello.example.com
  namespace: personal
spec:
  ref:
    name: hello
    kind: Service
    apiVersion: serving.knative.dev/v1
