# Managed resource mutation coverage

This catalogue records how a supported mutation family reaches the typed resource
inventory. It is an audit aid, not a declaration source: executable scope members,
reviewed operations, retained native bundles, and journal receipts remain the
authority.

Each row names the lifecycle-owning scope, declaration compiler, native executor,
focused evidence, delegated work, and disposition of the older entry point. A row
is `migrated` only when the public entry point uses the shared planner/executor and
the retained transport refuses an unscoped or foreign adapter child. `adapter-ready`
means the typed adapter exists but production command registration is still open.
EP-147 and EP-148 extend this table; EP-150 owns the final completeness audit.

| Mutation family | Owner scope | Declaration compiler | Executor / retained transport | Test evidence | Delegation | Legacy disposition | Status |
| --- | --- | --- | --- | --- | --- | --- | --- |
| Scoped review observation and operation selection | changed application, data, or platform scope plus changed shared-owner outputs | `Nagare.Inventory.Plan.affectedManagedIds` selects replaced or retired members, changed effective declarations, contribution targets, required broker topics, and bootstrap dependencies | Planner observes and classifies only selected members while composition still checks all accepted claims | `InventoryTransactionSpec` proves app A update excludes app B and an unrelated Pulumi resource; namespace-contribution and bootstrap verification cases remain covered | Unchanged foreign scopes retain their accepted declarations without provider reads | Full command and provider isolation proof remains open in M4 | partial |
| Retained stateless Kubernetes collection | retained historical owner scope | `CollectRetained` selects an exact historical incarnation after policy and consumer checks | `Nagare.Inventory.Adapters.Kubernetes` deletes namespaced ConfigMaps, Services, CronJobs, DomainMappings, and delete-policy PVCs with UID/resourceVersion preconditions, then verifies absence and records a tombstone; an unready owned route can be collected at its exact digest and version | `InventoryKubernetesSpec` proves disposable Service and CronJob collection, checks the DomainMapping conditional request and unready adapter path, and covers ConfigMap recording, disposable, and stale-precondition probes; a disposable DomainMapping stale-version refusal and exact deletion plus a disposable PVC create/conditional collection passed at the native API boundary | Kubernetes DeleteOptions applies the server preconditions; child resources are orphaned | `inventory collect --resource` is reviewed; StatefulSet and durable-data collection remain unsupported | partial |
| Pulumi cloud stack preview/apply, including APIs, IAM, network, disks, buckets, DNS/CDN, registry, VM, and cache resources | platform cloud foundation | `Nagare.Inventory.Cloud.compileCloudScope`; `infra/pulumi/src/resourceDeclarations.ts` consumes the canonical registration bundle | `Nagare.Inventory.Adapters.Pulumi` + `PulumiRuntime`; exact Pulumi saved plan | `InventoryCloudSpec` records preparation/apply/verification and physical observation; `infra/pulumi/test/resourceInventory.test.ts`; existing reviewed-plan guard fixtures | Pulumi provider bookkeeping is explicitly classified in the native registration bundle | `infra preview --inventory` plans through the shared executor; `infra apply` auto-detects inventory reviews; resume reconstructs the runtime. The no-`--inventory` compatibility path remains until production declaration generation and upgrade callers migrate | migrated-with-compatibility |
| Google CDN application DNS and shared cache policy | platform Pulumi scope plus pending per-host DNS owner | `Nagare.Cdn.Provision.planCdn` allows only the standing Google cache policy and lists per-host Cloud DNS writes; reviewed application and site scope compilers still refuse CDN | Pulumi alone writes the shared backend cache policy. The direct deploy path still writes host DNS through `gcloud` and has no reviewed DNS claim | `cdnProvisionTests` checks that Google plans contain only DNS and refuse per-app TTL, cache-mode, and path-rule overrides | Google backend, URL map, certificate, and apex DNS are platform-owned; per-host DNS ownership remains unresolved | Add an explicit provider account/zone/hostname claim and guarded DNS adapter before enabling reviewed CDN deploy | partial |
| Guarded NixOS activation | platform host | `Nagare.Inventory.Host.compileHostScope` | `Nagare.Inventory.Adapters.Host` + `HostRuntime`; `scripts/inventory-host-transport.sh`; self-reverting activation transport | `InventoryHostSpec`; `nix/checks/scripts/test-host-switch-identity.sh`; host auto-rollback VM check | Nix derivations/files remain internal to the host executor; preparation retains the evaluated closure | Typed inventory plan/apply/resume use the production runtime; upgrade/`just host-switch` compatibility routing remains EP-150 work | migrated-with-compatibility |
| VM start/stop | platform host | Host resource bundle (physical instance dependency and explicit operation are still to be added) | Host adapter; `scripts/vm-power.sh` | `scripts/test-inventory-transport-guards.sh`; existing project-confinement checks | GCE performs the power transition | Transport rejects a foreign inventory child; public recipes remain legacy | adapter-ready |
| NixOS image object and GCE image publication | platform artifacts | `Nagare.Inventory.Artifact.compileArtifactScope` | `Nagare.Inventory.Adapters.Artifact` + `ArtifactRuntime`; `scripts/inventory-artifact-transport.sh` | `InventoryArtifactSpec`; `scripts/test-upload-images.sh`; bucket ownership guard | Nix build and builder-side upload are bounded transport; Pulumi owns the destination bucket | Typed inventory plan/apply/resume require the reviewed destination/content digest; direct publication remains an EP-150 compatibility path | migrated-with-compatibility |
| Temporary/on-demand Nix builder setup and lifecycle | platform artifacts | Artifact resource bundle | Artifact adapter; `scripts/setup-nix-builder.sh` and builder proxy/power transport | `scripts/test-inventory-transport-guards.sh`; builder confinement assertions in `scripts/test-upload-images.sh` | Idle shutdown and IAP proxying remain delegated to the builder | Setup transport rejects a foreign inventory child; direct setup and proxy-triggered power remain legacy | adapter-ready |
| Attic/Nix cache OCI image publication | platform artifacts | Artifact resource bundle | Artifact runtime; `scripts/inventory-artifact-transport.sh`; bounded publisher | `InventoryArtifactSpec`; `scripts/test-inventory-transport-guards.sh`; immutable pin/digest checks in the publisher | Registry authentication and copy are bounded transport | `just nix-cache-publish` enters the reviewed bootstrap path; the low-level publisher requires the artifact adapter marker and exact reviewed destination and source digests | migrated |
| Cache database, Attic workload, named cache, and client public key | platform cache and cache-image | `Nagare.Inventory.Components.PackagedCache.compilePackagedCache` binds the released pin/archive and composes the image publication plus 16 cache resources | Artifact, Kubernetes, and named-cache adapters; cache members wait for reviewed image publication | `InventoryCacheSpec` checks complete scope, private native bytes, subprocess boundary, and packaged candidate; disposable database transaction exercises resume | PostgreSQL data and Attic signing identity are retained; client key is a typed generated output | `just nix-cache-bootstrap` and the standalone installer enter the reviewed bootstrap path when enabled | partial |
| Standalone database creation and application deploy databases | standalone or application database scope | `Nagare.Inventory.DataService.compileStandaloneDatabase` and `Nagare.Inventory.Application.compileApplicationDatabases` reuse `Nagare.Inventory.Database.compileDatabaseForBackend` with complete typed values; application preview includes the data-free credential template and retained backup, and reviewed `db create` composes a standalone scope from the same typed input as direct create after checking the accepted foundation Namespace identity and native address | Explicit-recovery `db create` publishes and applies through the shared journal or saves a review for `inventory apply`; `db retire --save-plan` verifies the accepted standalone StatefulSet name and namespace and retains all provider resources; direct delete remains active; legacy data operations refuse inventory transaction re-entry and accepted or retained companion addresses | `InventoryKubernetesSpec` covers complete standalone membership, wrong-scope refusal, native binding, and disposable resume; `InventoryApplicationSpec` checks complete application database membership and missing recovery refusal; `AppDeploySpec` checks complete preview membership and absent credential data | Database credential data and PVC have `Retain` lifecycle; reviewed retirement retains all provider resources | Migrate recovery-free direct create, route application databases, and implement reviewed native deletion before retiring direct delete | partial |
| Standalone Redpanda broker and logical topics | standalone broker scope | `Nagare.Inventory.DataService.compileStandaloneBroker` binds PVC, Service, StatefulSet, and each topic as a durable broker-scoped claim; reviewed `broker create` checks the accepted foundation Namespace and native address | Kubernetes executes the three native members; `BrokerRuntime` lists, creates, describes, and verifies topics with a guarded context and exact broker target. Explicit-recovery create uses the shared journal in one invocation or saves a review; explicit-to-explicit retention updates require saved review and a separate `inventory apply`; direct commands remain active but refuse accepted or retained companion addresses | `InventoryApplicationSpec` checks native/topic membership, retention update preflight and refusal, and the absent, unowned, and uncertain topic paths; `InventoryTransactionSpec` checks verification before a new consumer | PVC and topics have `Retain` lifecycle and explicit recovery intent; topic deletion is not automatic, and uncertain retention updates require manual recovery | Migrate recovery-free direct create and add reviewed native deletion and other topic setting changes before retiring direct commands | partial |
| Database backup and restore | standalone or application database scope | Reviewed `SnapshotData` and `RestoreData` operations still require backup identity, affected data incarnation, and recovery proof | `Nagare.Database.Backup` and `Nagare.Database.Restore` still submit direct Jobs; both refuse inventory transaction re-entry and accepted or retained database companion addresses | Existing backup/restore tests cover direct job rendering; reviewed operation tests remain open | Kubernetes Job completion and object-store writes are provider effects | Route backup, pruning, and restore through the reviewed operation journal before retiring direct commands | legacy |
| Application volume snapshot and restore | application or standalone service scope | Reviewed `SnapshotData` and `RestoreData` operations still require PVC identity, object address, and write fencing | `Nagare.Storage.Snapshot` and `Nagare.Storage.Restore` still submit direct Jobs; both refuse inventory transaction re-entry and a PVC address held by accepted or retained inventory | Existing storage tests cover direct job rendering; reviewed operation tests remain open | Kubernetes Job completion and object-store writes are provider effects | Route snapshot, pruning, and restore through the reviewed operation journal before retiring direct commands | legacy |
| Manual task run/delete and database/broker restart | independent manual Job scope plus application or standalone data scope | `task run --run-id` compiles one Job from the exact accepted CronJob template into a stable independent scope, including accepted unlabeled tasks addressed with `-`; it waits for completion and supports conditional collection after retirement. Other reviewed operational requests still need accepted bindings and stable operation identity | Stable-ID Job creation publishes and applies through the shared review and Kubernetes adapter in one invocation, or saves the review with `--save-plan`; direct task run/delete and database/broker restart reject inventory transaction re-entry; database/broker restart and interactive database shell refuse accepted or retained companion addresses, while direct task run/delete refuse owned CronJobs or run-history ConfigMaps | `InventoryApplicationSpec` checks Job identity, source digest, app label, and native spec; `InventoryKubernetesSpec` checks Job collection preconditions and background pod cleanup request; a disposable cluster created and conditionally deleted a Job; end-to-end saved task execution remains open | Kubernetes creates pods after Job submission | Add reviewed task deletion and data actions, cover pre-deploy hooks, and retire direct command mutations | partial |
| Application environment and Secret stores | application scope plus separate environment intent channels | Typed runtime Secret references require exact accepted Secret dependencies; merged or exact Runtime, Build, and Preview ConfigMap and versioned exact or single-key Secret channels have independent scope revisions | `env set/delete/sync --reviewed`, versioned `secret set/delete/sync`, and their `--save-plan` variants use the same immutable review service and Kubernetes adapter; direct unversioned Secret and unreviewed env writes refuse selected native addresses in accepted or retained inventory; legacy reads distinguish confirmed absence from permission/transport failure and reject malformed data values | `InventoryApplicationSpec` checks channel identities, native addresses, private Secret bytes, and rotation refusal; `AppDeploySpec` checks runtime Secret bindings; env-store tests reject failed and malformed reads | Kubernetes holds the store values; secret bytes stay out of public review | Retire direct mutations after preview deployment has its own reviewed scope | partial |
| Standalone web Service with accepted databases and no CDN effects | standalone service scope | `Nagare.Inventory.Application.compileStandaloneServiceWithRelease` composes the Service/PVC/DomainMapping and scheduled CronJobs with a mandatory per-Service release-history ConfigMap; retained PVCs require recovery intent, supplied TLS requires an exact typed Secret dependency, broker Service/topic references bind accepted standalone claims, and protected routes claim a central DomainMapping plus an auth-owner backend contribution. Database connections bind an accepted standalone Service, StatefulSet, and private credential template in one scope | `deploy --save-plan` binds the accepted OCI publication, foundation Namespace, exact database and broker dependencies, accepted auth owner, and accepted private release history to native bytes for `inventory apply`; an explicit import proposal can adopt a matching old log; direct deploy refuses owned Service, CronJob, or release-history addresses; `app delete --save-plan` can retire the accepted standalone scope while retaining its members | `AppDeploySpec` checks standalone membership, schedules, supplied-TLS Secret and broker bindings, topic environment and ordering, accepted auth contribution/default route, database connection fields, database ordering, missing private credential refusal, and release-history ordering | Knative controller children remain reserved by the Service claim | Add CDN decisions and legacy access-route transition before retiring direct deploy | partial |
| Standalone worker Deployment and PVCs | standalone worker scope | `Nagare.Inventory.Application.compileStandaloneWorkerWithDependencies` reuses the application worker binder; retained PVCs need explicit recovery, runtime Secret references need accepted dependencies, and broker Service/topic and database references bind accepted standalone scopes | `worker deploy --save-plan` binds an accepted Namespace, exact OCI publication, topic claims, and database dependencies; `worker delete --save-plan` selects the accepted Deployment before reviewed retirement, preserving retained PVCs | `AppDeploySpec` covers standalone native membership, recovery refusal, accepted topic environment and ordering, database connection and Secret references, and retirement selection by address and scope key | Kubernetes owns derived Pods | Retire direct worker deploy after remaining command migration | partial |
| Static/server site deploy, rollback, and preview lifecycle | independent site or preview scope | `compileStaticSiteScope` and `compileServerSiteScope` bind supported production sites' Service, domains, release history, and server PVCs to exact native bytes and accepted Namespace/image identities; server Runtime and site supplied TLS Secret refs require accepted typed dependencies; static and server preview compilers bind their Service and domain after the exact four accepted overlay stores, with server Runtime Secret dependencies and preview PVCs with explicit recovery | Accepted-image static and server production deploys and previews publish/apply through the shared journal or save reviews for `inventory apply`; adoption of current direct members requires a saved review; `site rollback --save-plan` selects accepted private history and image publication; `site preview delete --save-plan` retires the exact preview scope before a conditional collection review; retained server PVCs require explicit recovery; direct Knative mutation remains for other site paths, whose Service, domain, release-history, and written PVC address guards refuse accepted or retained ownership | Static/server scope membership, release/import/rollback, retained-volume, Secret dependency, accepted preview-store, preview retirement and conditional Knative Service collection request, and retained history guard checks plus existing site rendering tests; disposable Knative Service stale/exact delete probe passed, while end-to-end inventory collection remains open | Knative creates derived revisions and Services | Add Build/Preview Secret publication, CDN, and operational site actions; collect previews in dependency order | partial |
| Application and static release history ConfigMaps | application, standalone Service, or site scope | `compileApplicationScope`, `compileStandaloneServiceWithRelease`, and both site compilers require exact legacy release ConfigMaps after their workloads | Reviewed app, Service, static site, and supported server site commands read prior entries only from accepted private native evidence and offer exact adoption of legacy logs; direct writes remain for legacy commands | Scope membership and release checks, history and import round trips, strict release-log reads, and disposable Kubernetes absence evidence | Kubernetes stores the history object | Retire remaining direct writes after complete site coverage | partial |
| Application Knative Service, databases, attached PVCs, DomainMappings, worker Deployments/PVCs, and Service-attached scheduled CronJobs | application scope plus per-tag hook scopes | `Nagare.Inventory.Application.compileApplicationScope` binds native bytes, stable keys, PVC recovery, database ordering, connection fields, credential Secret references, accepted broker Service/topic dependencies, accepted auth-owner backend contributions and central routes, external Secret dependencies, and cross-component claims; `app deploy --save-plan` composes supported workloads and schedules against an accepted or granted Namespace and OCI image. Aggregate pre-deploy Tasks bind independent per-tag Job scopes and completion operations with explicit affected-resource sets | Saved deploy and `app delete --save-plan` retirement reviews execute through `inventory apply`; retirement retains managed members. Direct app deploy, restart, stop, and delete, plus single-Service and worker deploy, refuse native addresses already in accepted or retained history | `AppDeploySpec` covers native env, schedules, hook effect and Job proof ordering, scope composition, recovery, Secret and broker topic binding refusal, central access route and portal ordering, native workload and retirement selection guards, namespace grants, and cross-app claims; `InventoryTransactionSpec` checks affected-data, Job, proof, and workload ordering; `InventoryArtifactSpec` checks accepted image identity | Knative reserves its derived core Service; net-certmanager provisions automatic origin TLS | Add CDN contributions, operational overrides, legacy access-route transition, and default reviewed planning before removing direct deploy | partial |
| Protected Service access routes and shared auth settings | application or standalone Service scope for DomainMappings and contributions; platform auth for backend and Shomei ConfigMaps | `acceptedAccessBinding` proves the accepted owner grants, base domain, and enforcer; `compileServiceMembers` declares central routes and `RegisterBackend` contributions | Kubernetes adapter applies the app-owned DomainMapping and owner-composed ConfigMaps; retained DomainMappings use conditional reviewed collection even when an origin is unready | `AppDeploySpec` covers protected and portal route composition, default host, dependency ordering, missing binding, and scope isolation; `InventoryKubernetesSpec` checks the conditional and unready DomainMapping paths; disposable serving API preconditions passed | Enforcer and Shomei remain platform auth workloads | Direct Service/app deploy and app delete refuse legacy access writes while the auth map is accepted or retained; legacy route adoption and end-to-end retained collection remain open | partial |
| Host-refreshed registry pull and forge credentials | NixOS host timer authority | `nixos/hosts/nagare-01/registries.nix` fixes registry scope to `personal` and `nagare-system`; `forge-credentials.nix` fixes read/write Secret names in its configured namespace (default `personal`) | Registry timer refreshes only `nagare-registry-pull` and patches only the default ServiceAccount's pull reference in those two namespaces; forge timers refresh only `nagare-forge-read` and `nagare-forge-write` | Nix evaluation and generated shell syntax; `scripts/test-forge-credentials-refresh.sh` checks annotation, short-expiry refusal, and foreign owner refusal; disposable k3d stale Secret replace and ServiceAccount merge-patch checks | Both timers stamp a source SHA-256 and expiry on their Secrets; the registry timer stamps its source version on the ServiceAccount. These host-owned values are delegated rotation, with no inventory deletion or adoption grant | Host units remain the credential authority; cluster bootstrap does not declare these Secrets or ServiceAccounts as direct owned resources | delegated |
| Platform Namespaces and personal Job quota | platform foundation | `Nagare.Inventory.Components.Foundation.compileFoundation` | Kubernetes adapter; canonical native objects | `InventoryFoundationSpec` checks three claims, quota ordering, composition, and native binding; disposable cluster verifies guarded Namespace label update | Upstream cert-manager, Knative, and Kourier manifests own their own Namespaces | `cluster-bootstrap`, `local-bootstrap`, and `job-runs-bootstrap` call `scripts/run-reviewed-bootstrap.sh`, which plans and applies the retained inventory review | migrated |
| Pinned cert-manager, issuer chain, Knative Serving, Kourier, and net-certmanager manifests | separate upstream platform scopes plus net-controller-image | `Upstream.configuredUpstreamInputsWithIssuer`, `ControllerImage.compileControllerImage`, and `Upstream.compileUpstream`; packaged manifest, archive, and OCI digests bind to the candidate | Artifact adapter publishes the patched controller to the selected registry before the Kubernetes adapter changes its reviewed Deployment; CRDs, Deployments, Certificates, and ClusterIssuers wait for readiness | `InventoryUpstreamSpec` checks controller publication identity, missing archive refusal, image binding and ordering, issuer policy, and scope composition; `InventoryKubernetesSpec` checks readiness; transport child guard passes; disposable full bootstrap and accepted replay converged | Controller-populated webhook Secret data and generated workloads remain delegated | `cluster-bootstrap` and `local-bootstrap` call the reviewed bootstrap path; direct controller import transport remains a bounded legacy helper | partial |
| Auth databases, credentials, migrations, workloads, and shared settings | platform auth | `Nagare.Inventory.Components.PackagedAuth.packagedAuthInputs` supplies context inputs to `Auth.compileAuthComponent`, including two typed database bundles and digest-addressed image references; the closed portal contribution composes both backend and Shomei ConfigMaps | Kubernetes adapter creates auth Secret data only from closed templates after confirmed absence; revision-named Jobs have reviewed `SchemaMigration` proof operations; Deployments and Knative Service wait for readiness; generated ConfigMap bytes are rebound from typed contributions before mutation | `InventoryAuthSpec` checks database composition, immutable image refusal, proof edges, local bucket ordering, shared settings, and changed-byte refusal; `InventoryKubernetesSpec` checks generated credential keys and refusal of unknown templates | PostgreSQL data and auth keys are protected; old Job collection still needs lifecycle policy | `auth-install.sh` and `local-auth/install.sh` delegate to the reviewed bootstrap path; the legacy application access command remains EP-148 work | partial |
| Helm releases and direct observability resources | five release scopes plus observability-extra and observability-secrets | `Observability.compilePinnedObservability` binds five vendored charts and values; `ObservabilityExtras` binds pinned VM scrapes/rule and Grafana ConfigMaps; `ObservabilitySecrets` decrypts, validates, and normalizes context-owned Secrets to private native `data` before review | Helm 4 adapter with exact postrenderer gate; Kubernetes adapter owns chart CRDs, direct extras, and private Secret members; metrics release waits for Secrets, extras wait for its CRDs | `InventoryObservabilitySpec`; `scripts/test-helm-review-renderer.sh`; disposable k3d Helm create/upgrade/verify and Secret create/update/verify; chart workload checks render vendored archives | Helm hooks are claimed directly; operator-generated descendants remain delegated | `just observability` and `cluster/observability/install.sh` enter the reviewed bootstrap path | partial |
| Local MinIO endpoint and backup bucket | platform local-object-store | `Nagare.Inventory.Components.LocalObjectStore.compileLocalObjectStore` binds the packaged manifest digest and the selected local backup target, replacing the legacy fixed Secret values with reviewed templates | Kubernetes adapter generates the primary credential on guarded create, then copies its values to the `personal` Secret after checking context and source identity; it also owns Deployment, Service, and bucket preparation Job | `InventoryAuthSpec` checks five native members, absence of fixed values, generated/copy data, source identity refusal, profile mismatch refusal, and auth dependency on bucket preparation; disposable Kubernetes execution creates and verifies both Secrets with identical data | Local disposable object data lives in MinIO's `emptyDir` | `just local-minio` enters the reviewed bootstrap path; standalone manifest remains only a pinned source | migrated |
| Bootstrap release marker | platform bootstrap-stamp | `Nagare.Inventory.Bootstrap.compileBootstrapStamp` binds the selected payload identity and plan-time timestamp to a ConfigMap | Kubernetes adapter; the marker waits for all composed changes, declared operations, and read-only verification of unchanged accepted Kubernetes objects and Helm releases | `InventoryFoundationSpec` checks address and native membership; `InventoryObservabilitySpec` checks complete cloud ordering, interruption/resume, and accepted replay; `InventoryKubernetesSpec` checks a live-drift repair and stale read-only verification | The marker is the last direct bootstrap member; no controller owns it | `platform bootstrap plan` includes it in the review; `platform stamp` now refuses and directs callers to reviewed bootstrap. Supported bootstrap just recipes use the reviewed path. | migrated |
| Context image/state bucket and API bootstrap | platform cloud foundation | Cloud resource bundle plus explicit bootstrap operation | Pulumi adapter after reviewed local bootstrap; store migration is owned by EP-151 | Bucket ownership guard; Pulumi registration parity | GCS conditional history migration belongs to EP-151 | `nagarectl init` still performs pre-inventory bootstrap; it must not be removed before the first-context local transaction exists | adapter-ready |
| Global release payload publication | dedicated release-publication scope | Artifact external-reference types only in deployment contexts | GitHub release adapter owned by EP-150 | To be supplied by EP-150 | Consuming contexts hold external references and never own publication | Not migrated by EP-146 | planned |

## Adapter-child boundary

The executor exports both `NAGARE_INVENTORY_TRANSACTION` and the closed executor
token in `NAGARE_INVENTORY_ADAPTER_CHILD` for execute, verify, and recovery calls,
then restores the caller's environment. Retained cloud/host/artifact transports
check the token before context resolution or provider work. A nested inventory
command sees the transaction marker and refuses, preventing self-deadlock on the
context lock. `scripts/test-inventory-transport-guards.sh` covers the early shell
boundary, while `InventoryTransactionSpec` covers marker scope and lock re-entry.
