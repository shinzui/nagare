-- | Prototype 2 — embedded interpreter. Stub until M3 (the @hint@ vs. GHC-API
-- feasibility investigation picks the mechanism and wires the dependency).
module Main (main) where

main :: IO ()
main = putStrLn "proto2-interpreter: not yet implemented (see EP-8 M3)"
