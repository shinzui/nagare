-- | Prototype 3 — Dhall. Stub until M4 (adds the @dhall@ dependency and the
-- corpus package path, then decodes @hello/hello.dhall@).
module Main (main) where

main :: IO ()
main = putStrLn "proto3-dhall: not yet implemented (see EP-8 M4)"
