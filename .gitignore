.claude/
.agents/
.seihou/manifest.json.tmp
CLAUDE.local.md

# Per-operator GCP target profile (EP-60). The tracked schema/example is
# nagare.target.env.example; the real, per-operator file is never committed so
# the repository stays project-agnostic.
nagare.target.env

# Per-operator LOCAL-mode profile (EP-82). The tracked schema/example is
# nagare.local.env.example; the real, per-operator file is never committed.
nagare.local.env

# Nix build outputs
result
result-*

# direnv cache
.direnv/

# Cabal workspace outputs
dist-newstyle/
.ghc.environment.*

# Pulumi in-repo home, state backend, and TypeScript deps (see .envrc / EP-2)
infra/pulumi/.pulumi-home/
infra/pulumi/.pulumi-state/
infra/pulumi/node_modules/

# Never commit age private keys (EP-7)
keys.txt
*.agekey
*.age.key

# EP-6 live-test harness kubeconfig + tunnels
.live-test/

.mina/
